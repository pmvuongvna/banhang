{
  "name": "qlbh-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute qlbh-db --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  }
}
